dependencyResolutionManagement {
    repositories {
        // Use Maven Central for resolving dependencies.
        mavenCentral()
    }
}

include 'morphir', 'account-service','position-service','trade-processor','trade-service','database'